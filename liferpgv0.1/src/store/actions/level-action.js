import { taskActions } from '../task-slice';
import { useSelector } from 'react-redux';

// export const getLevelAction = id => {
//   return dispatch => {
//     const levelData = useSelector(state => state.levelData.levels);

//     dispatch(taskActions.getLevel({ lvlData: levelData, id: id }));
//   };
// };
